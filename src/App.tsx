import Routing from './routes/index';
import GlobalStyle from 'shared/style/globalStyle';

const App = () => {
    return (
        <>
        <GlobalStyle/>
        <Routing />
        </>
    );
};

export default App;
